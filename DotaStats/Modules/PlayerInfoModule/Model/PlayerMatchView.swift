import Foundation

struct PlayerMatchView {
    let matchId: Int
    let win: Bool
    let duration: Int
    let heroId: Int
    let kills: Int
    let deaths: Int
    let assists: Int
    let skill: String
    let gameMode: Int
}
